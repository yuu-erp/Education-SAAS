import appConfig from './app.config';
import databaseConfig from './database.config';

export type { AppConfig, DatabaseConfig, AllConfigType } from './config.type';

export { appConfig, databaseConfig };
