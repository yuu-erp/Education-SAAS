import { Injectable } from '@nestjs/common';

/**
 * TODO:
 * 1. Install @nestjs/passport and passport-jwt
 * 2. Extend this class with AuthGuard('jwt') or link it to the global one in src/common/guards
 * 3. Use Reflector to handle @Public() decorator bypass logic
 */
@Injectable()
export class JwtAuthGuard {
  // Placeholder for real JWT Auth Guard implementation
}
